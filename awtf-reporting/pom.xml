<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.pcalouche</groupId>
    <artifactId>awtf-reporting</artifactId>
    <version>2.0.0</version>
    <packaging>jar</packaging>
    <name>AWTF Reporting Project</name>

    <properties>
        <!-- Misc Properties -->
        <java.version>1.8</java.version>
        <!-- Dependency Versions -->
        <awtf-core.version>2.0.0</awtf-core.version>
        <awtf-app-example.version>2.0.0</awtf-app-example.version>
        <jackson.version>2.8.3</jackson.version><!-- Sep 2016 -->
        <!-- Plugin Versions -->
        <maven.compiler.version>3.5.1</maven.compiler.version><!-- Feb 2016 -->
        <maven.surefire.version>2.19.1</maven.surefire.version><!-- Dec 2015 -->
    </properties>

    <dependencies>
        <!-- AWTF Core -->
        <dependency>
            <groupId>com.pcalouche</groupId>
            <artifactId>awtf-core</artifactId>
            <version>${awtf-core.version}</version>
        </dependency>
        <!-- AWTF App Example -->
        <dependency>
            <groupId>com.pcalouche</groupId>
            <artifactId>awtf-app-example</artifactId>
            <version>${awtf-app-example.version}</version>
        </dependency>
        <!-- Jackson Databind -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.version}</version>
                <configuration>
                    <includes>
                        <include>**/Test*.java</include>
                        <include>**/*Test.java</include>
                        <include>**/*TestCase.java</include>
                        <include>**/*TestRunner.java</include>
                    </includes>
                </configuration>
            </plugin>
        </plugins>
        <!-- Copy all features files from the projects you care about to this project that you care about here. -->
        <testResources>
            <testResource>
                <directory>../awtf-core/src/test/resources/</directory>
                <targetPath>awtf-core</targetPath>
                <includes>
                    <include>**/*.feature</include>
                </includes>
            </testResource>
            <testResource>
                <directory>../awtf-app-example/src/test/resources/</directory>
                <targetPath>awtf-app-example</targetPath>
                <includes>
                    <include>**/*.feature</include>
                </includes>
            </testResource>
            <testResource>
                <directory>src/main/resources/awtfReport</directory>
                <targetPath>../awtfReport</targetPath>
            </testResource>
        </testResources>
    </build>
</project>
